<Window x:Class="Natariys.windows.WindowLogin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Natariys.windows"
        mc:Ignorable="d"
        Title="Авторизация" MinHeight="200" Height="0" MinWidth="400" Width="0" WindowStartupLocation="CenterScreen"
        FontSize="16" FontWeight="SemiBold">
    <Grid>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="Margin" Value="0 0 0 5"/>
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="Padding" Value="2"/>
                </Style>
            </Grid.Resources>

            <TextBlock Text="Логин:" Grid.Row="1"/>
            <TextBlock Text="Пароль:" Grid.Row="2"/>

            <TextBox x:Name="__login" Grid.Column="1" Grid.Row="1"/>
            <PasswordBox x:Name="__password" Grid.Column="1" Grid.Row="2" Margin="0 0 0 5" Padding="3 1">
                <PasswordBox.Resources>
                    <Style x:Key="{x:Type PasswordBox}" TargetType="{x:Type PasswordBox}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type PasswordBox}">
                                    <Border CornerRadius="3" BorderThickness="1" BorderBrush="DarkGray">
                                        <ScrollViewer x:Name="PART_ContentHost"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </PasswordBox.Resources>
            </PasswordBox>

            <StackPanel Grid.ColumnSpan="2" Grid.Row="3">
                <Button Content="Вход" Width="100" Click="Button_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
